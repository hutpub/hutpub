<svg fill="none" viewBox="0 0 200 50" width="200" height="50" xmlns="http://www.w3.org/2000/svg">
    <text id="myText" x="10" y="30" font-family="Arial" font-size="20" fill="blue">
        My Animated Text
    </text>

    <script type="application/ecmascript">
        <![CDATA[
            function animateText() {
                var textElement = document.getElementById('myText');
                var currentColor = textElement.style.fill;

                if (currentColor === 'blue') {
                    textElement.style.fill = 'yellow';
                } else {
                    textElement.style.fill = 'blue';
                }
            }

            setInterval(animateText, 1000);
        ]]>
    </script>
</svg>
